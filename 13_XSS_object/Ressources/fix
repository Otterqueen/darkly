Lorsqu'on utilise une requête GET en back, l'utilisateur peut facilement modifier les paramètres envoyés. Il faut donc rajouter des vérifications, notamment de la provenance en cas de média, ou fonctionner avec un dictionnaire de variables, voir éviter les requêtes GET pour afficher un média.
